from setuptools import setup, find_packages

setup(
        name='telegram-bot',
        version='0.3.2',
        packages=find_packages(),
        license='TBD',
        long_description='TBD'
        )
